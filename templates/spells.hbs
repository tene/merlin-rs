<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8" />
    <title>Spells</title>
</head>

<body>
    <h1>Spells</h1>
    <div>
        <a href="/">Index</a>
    </div>
    <ul>
        {{#each spells}}
        <li>
            <a href="/spell/{{this.name}}">{{this.name}}</a>
        </li>
        {{/each}}
    </ul>
</body>

</html>